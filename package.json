{
  "name": "scenemap-lumiverse",
  "version": "1.0.1",
  "type": "module",
  "scripts": {
    "build": "bun build src/backend.ts --target bun --outfile dist/backend.js && bun build src/frontend.ts --target browser --outfile dist/frontend.js",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@cfworker/json-schema": "^1.9.0",
    "sortablejs": "^1.15.7"
  },
  "devDependencies": {
    "@types/sortablejs": "^1.15.9",
    "bun-types": "^1.3.10",
    "lumiverse-spindle-types": "0.6.3",
    "typescript": "^6.0.2"
  }
}
